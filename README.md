# House Staff - React E-commerce Application

Простое и стильное веб-приложение для покупки мебели, построенное на React. Приложение позволяет клиентам просматривать товары, выбирать категории, добавлять товары в корзину и просматривать полную информацию о товарах.

## 🎯 Возможности

- **Каталог товаров** — просмотр доступной мебели (стулья, столы, шкафы, кровати)
- **Фильтрация по категориям** — быстрый поиск товаров по типам
- **Корзина покупок** — добавление товаров, удаление, расчёт суммы
- **Подробная информация о товаре** — клик на товар открывает полное описание
- **Адаптивный дизайн** — удобно использовать на разных устройствах
- **Иконки из React Icons** — красивые иконки для интерфейса (FaShoppingBag и др.)

## 🛠 Технологический стек

- **React 19.2.4** — фреймворк для UI
- **React Scripts 5.0.1** — сборщик и dev-server
- **React Icons 5.5.0** — библиотека иконок
- **CSS** — стилизация (index.css)

## 📁 Структура проекта

```
first-react-app/
├── public/
│   ├── index.html           # Главная HTML-страница
│   └── img/                 # Изображения товаров
│       ├── styl.jpg         # Стул
│       ├── stol.jpg         # Стол
│       ├── skaf.jpg         # Шкаф
│       └── krovat.jpg       # Кровать
├── src/
│   ├── index.js             # Входная точка React приложения
│   ├── App.js               # Главный компонент приложения
│   ├── index.css            # Глобальные стили
│   ├── components/
│   │   ├── header.js        # Шапка сайта с корзиной и меню
│   │   ├── footer.js        # Подвал сайта
│   │   ├── categories.js    # Кнопки фильтра по категориям
│   │   ├── items.js         # Контейнер для списка товаров
│   │   ├── item.js          # Компонент одного товара
│   │   ├── order.js         # Элемент в корзине
│   │   └── showfullitem.js  # Модальное окно с полной инфой о товаре
│   └── images/              # Дополнительные изображения (логотипы, фоны)
├── package.json             # Конфиг проекта и зависимости
├── package-lock.json        # Зафиксированные версии зависимостей
└── README.md                # (этот файл)
```

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** (версия 14+) и **npm** (версия 6+)
- Git (опционально, для клонирования репозитория)

### Установка

1. **Клонируйте репозиторий** (или распакуйте архив):
   ```bash
   git clone https://github.com/thesharkhomevania2016-arch/project.git
   cd first-react-app
   ```

2. **Установите зависимости**:
   ```bash
   npm install
   ```

### Запуск в режиме разработки

```bash
npm start
```

Приложение откроется в браузере по адресу [http://localhost:3000](http://localhost:3000).
Страница будет перезагружаться при редактировании файлов.

### Сборка для production

```bash
npm run build
```

Создаёт оптимизированную сборку в папке `build/` для развёртывания.

### Запуск тестов

```bash
npm test
```

Запускает тесты в интерактивном режиме. В данном проекте тесты отсутствуют, но структура для них подготовлена.

## 🔧 Доступные команды

| Команда | Описание |
|---------|---------|
| `npm start` | Запустить dev-сервер (localhost:3000) |
| `npm run build` | Собрать проект для production |
| `npm test` | Запустить тесты (watch режим) |
| `npm audit` | Проверить уязвимости зависимостей |
| `npm audit fix` | Автоматически исправить уязвимости (безопасные) |

## 📦 Развёртывание

### Локальный запуск собранной версии

1. Установите глобальный сервер:
   ```bash
   npm install -g serve
   ```

2. Запустите собранное приложение:
   ```bash
   serve -s build
   ```

3. Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Развёртывание на хостинг

Папка `build/` содержит готовый статический контент. Вы можете:

- **Netilify**: перетащить папку `build/` на https://app.netlify.com/drop
- **Vercel**: запустить `vercel` следуя инструкциям
- **GitHub Pages**: настроить автоматическое развёртывание через Actions
- **Традиционный хостинг**: загрузить содержимое `build/` на FTP/SFTP сервер

Подробнее: https://cra.link/deployment

## 🐛 Известные проблемы и исправления

### ESLint предупреждения (неблокирующие)

При сборке видны предупреждения:
- `img elements must have an alt prop` — добавляем описание для изображений (доступность).
- `'showfullitem' is defined but never used` — удалить неиспользуемый импорт.

Эти предупреждения не мешают работе приложения, но рекомендуется их исправить для лучшей доступности и чистоты кода.

### Уязвимости безопасности

Проект имеет 3 moderate уязвимости (в dev-dependencies `webpack-dev-server`). Они:
- **не влияют на production-сборку** (только на dev-сервер)
- **не требуют срочного исправления** для локальной разработки
- Можно изучить: `npm audit` для полного отчёта

Для обновления используются `overrides` в `package.json`, которые смягчают проблемы.

## 📝 Основные компоненты

### App.js
Главный компонент, управляет:
- Состояние товаров и текущих товаров в каталоге
- Состояние корзины
- Фильтрацию по категориям
- Открытие/закрытие модального окна товара

### Header
Содержит:
- Логотип "House Staff"
- Меню навигации
- Иконка корзины (с количеством товаров)
- Выпадающий список товаров в корзине с суммой

### Categories
Кнопки для фильтрации по типам:
- All (все товары)
- chair (стулья)
- table (столы)
- sofa (шкафы)
- bed (кровати)

### Items / Item
- **Items** — контейнер, выводит список товаров
- **Item** — отдельный товар (изображение, название, цена, кнопка добавления)

### ShowFullItem
Модальное окно с полным описанием товара, отображается при клике на товар.

### Order / Footer
- **Order** — строка товара в корзине (название, цена, кнопка удаления)
- **Footer** — подвал сайта

## 🎨 Кастомизация

### Добавление нового товара

В `App.js` в массиве `items` добавьте объект:

```javascript
{
  id: 5,
  title: "Диван",
  img: "divan.jpg",
  desc: "Описание дивана...",
  category: "sofa",
  price: "45000.00",
}
```

Не забудьте добавить изображение в папку `public/img/`.

### Изменение стилей

Отредактируйте `src/index.css` для изменения цветов, размеров и макета.

## 📞 Поддержка и контакты

- **GitHub**: https://github.com/thesharkhomevania2016-arch/project
- **Email**: thesharkmeister@gmail.com (автор)

## 📄 Лицензия

Проект распространяется без явной лицензии. Используйте свободно в учебных целях.

---

**Версия**: 0.1.0  
**Последнее обновление**: 6 февраля 2026 г.  
**Автор**: thesh
